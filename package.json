{
  "name": "privamargin-cre",
  "version": "0.2.0",
  "private": true,
  "description": "Chainlink CRE workflow for PrivaMargin LTV monitoring and liquidation triggers",
  "type": "module",
  "scripts": {
    "postinstall": "bun x cre-setup",
    "build": "cre-compile src/workflow.ts",
    "simulate": "cre workflow simulate --target local-simulation --config src/config.json src/workflow.ts",
    "deploy": "cre workflow deploy",
    "test": "vitest"
  },
  "dependencies": {
    "@chainlink/cre-sdk": "^1.2.0",
    "viem": "^2.47.2"
  },
  "devDependencies": {
    "typescript": "~5.8.3",
    "vitest": "^3.0.0"
  }
}
